# Server
PORT=5000
NODE_ENV=development

# Mongo — local install or an Atlas connection string.
# Atlas: mongodb+srv://<user>:<password>@<cluster>.mongodb.net/portfolio?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://portfolioAdmin:Mrcs14324Shivi@cluster0.uy3x3ej.mongodb.net/portfolio?retryWrites=true&w=majority

# Auth. Production refuses to start on the placeholder or on anything under 32
# characters. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET==4c3fc9bbeeaf524adb4206b0f7d357db5002c2e77165e442f5af841ed11d4697
JWT_EXPIRES_IN=7d

# Comma-separated list of origins allowed to call the API.
# In production list the deployed frontend and admin URLs, with no trailing slash.
CORS_ORIGINS=https://myportfolio-adminshekhar.netlify.app,https://shekharmyportfolio.netlify.app,http://localhost:5173
# Public base URL of this API — used to build absolute upload URLs.
# In production this is the host's own URL, e.g. https://portfolio-api.onrender.com
PUBLIC_URL=http://localhost:5000

# Seeded admin account (npm run seed)
ADMIN_EMAIL=0810chandrashekhar@gmail.com
ADMIN_PASSWORD=Admin@12345
ADMIN_NAME=Chandrashekhar Ghosh
